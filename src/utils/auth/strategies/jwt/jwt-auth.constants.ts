export const CADENCE_AUTH_COOKIE_NAME = 'cadence-authorization';

/** gRPC metadata key the JWT travels under — unchanged from master. */
export const CADENCE_AUTH_GRPC_METADATA_KEY = 'cadence-authorization';
